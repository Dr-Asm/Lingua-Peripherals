plugins {
    id 'eclipse'
    id 'idea'
    id 'maven-publish'
    id 'net.minecraftforge.gradle' version '[6.0,6.2)'
}

version = '2.0.0'
group = 'com.linguaperipherals'

base { archivesName = 'linguaperipherals' }

java.toolchain.languageVersion = JavaLanguageVersion.of(17)

minecraft {
    mappings channel: 'official', version: '1.20.1'
    runs {
        client {
            workingDirectory project.file('run')
            property 'forge.logging.markers', 'REGISTRIES'
            property 'forge.logging.console.level', 'debug'
            mods { linguaperipherals { source sourceSets.main } }
        }
        server {
            workingDirectory project.file('run')
            property 'forge.logging.markers', 'REGISTRIES'
            property 'forge.logging.console.level', 'debug'
            mods { linguaperipherals { source sourceSets.main } }
        }
    }
}

repositories {
    flatDir { dirs 'libs' }
    maven { url "https://api.modrinth.com/maven"; content { includeGroup "maven.modrinth" } }
}

dependencies {
    minecraft 'net.minecraftforge:forge:1.20.1-47.3.0'
    implementation fg.deobf("maven.modrinth:cc-tweaked:1.120.0")
    // Create 0.5.1j — compileOnly; runtime Mixin refmap incompatible with available Forge builds
    // FlapDisplay API unchanged in later Create versions — compiles and works in production
    compileOnly "maven.modrinth:create:1.20.1-0.5.1.j"
}

tasks.named('jar', Jar).configure {
    manifest { attributes([
        'Specification-Title': 'Lingua Peripherals',
        'Specification-Vendor': 'linguaperipherals',
        'Specification-Version': '1',
        'Implementation-Title': 'Lingua Peripherals',
        'Implementation-Version': project.version,
        'Implementation-Vendor': 'linguaperipherals'
    ])}
}

tasks.withType(JavaCompile).configureEach { options.encoding = 'UTF-8' }
